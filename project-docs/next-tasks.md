# Próximas tarefas

1. Revisar modelo **`reviews`** + domínio para **ponderação** (migrações quando a regra existir).
2. **Anti-fraude** + sinais **Maps/Places** (desenho antes de codar pesado).
3. **`users`** + auth + FK em `reviews.user_id`.
4. **GET** reviews por `place_id` (+ paginação, sqlc, testes).
5. **OpenAPI** dos contratos JSON.
6. Cliente **Places** em `internal/integration/…` (timeouts, mocks).
7. **`place_scores`** + recência (depois da ponderação).
8. **Health** mais rico só se precisar para deploy.

Atualizar este arquivo e `current-state.md` ao fechar itens.
